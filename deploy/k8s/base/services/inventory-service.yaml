apiVersion: v1
kind: ServiceAccount
metadata: { name: inventory-service, namespace: ecommerce-saga }
---
apiVersion: v1
kind: ConfigMap
metadata: { name: inventory-service-config, namespace: ecommerce-saga }
data:
  NODE_ENV: production
  INVENTORY_SERVICE_PORT: '3002'
  KAFKA_BROKERS: saga-kafka-kafka-bootstrap:9092
  KAFKA_CLIENT_ID_PREFIX: ecommerce
---
apiVersion: v1
kind: Secret
metadata: { name: inventory-service-secrets, namespace: ecommerce-saga }
type: Opaque
stringData:
  # Valores FICTÍCIOS, iguais ao .env.example — troque antes de qualquer uso real.
  INVENTORY_DATABASE_URL: postgresql://inventory_svc:changeme@saga-postgres-rw:5432/inventory_db?schema=public
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: inventory-service, namespace: ecommerce-saga }
spec:
  replicas: 2
  selector: { matchLabels: { app: inventory-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxUnavailable: 0, maxSurge: 1 }
  template:
    metadata: { labels: { app: inventory-service } }
    spec:
      serviceAccountName: inventory-service
      terminationGracePeriodSeconds: 45
      containers:
        - name: inventory-service
          image: ecommerce-saga-inventory-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 3002 }]
          envFrom:
            - configMapRef: { name: inventory-service-config }
            - secretRef: { name: inventory-service-secrets }
          lifecycle:
            preStop:
              exec: { command: ['sh', '-c', 'sleep 5'] }
          startupProbe:
            httpGet: { path: /health/startup, port: 3002 }
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: { path: /health/ready, port: 3002 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health/live, port: 3002 }
            periodSeconds: 10
          resources:
            requests: { memory: 128Mi, cpu: '100m' }
            limits: { memory: 256Mi, cpu: '250m' }
---
apiVersion: v1
kind: Service
metadata: { name: inventory-service, namespace: ecommerce-saga }
spec:
  selector: { app: inventory-service }
  ports: [{ port: 3002, targetPort: 3002 }]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: inventory-service, namespace: ecommerce-saga }
spec:
  minAvailable: 1
  selector: { matchLabels: { app: inventory-service } }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: inventory-service, namespace: ecommerce-saga }
spec:
  podSelector: { matchLabels: { app: inventory-service } }
  policyTypes: [Ingress, Egress]
  ingress: [] # não recebe tráfego externo — só o Kafka aciona (e o pod é quem PUXA do Kafka, não o inverso)
  egress:
    - to: [] # Kafka e Postgres são outros pods do MESMO namespace — sem seletor, permite tudo dentro; troque por matchLabels do saga-kafka/saga-postgres para trancar mais
